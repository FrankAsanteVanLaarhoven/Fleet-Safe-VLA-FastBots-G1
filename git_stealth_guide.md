# 🎯 Iron Cloud Nexus AI - Git Stealth Assistance Guide

## 🚀 **Complete Stealth Assistance System for Outlier Git Screening**

### **📋 Overview**
This guide provides you with **multiple stealth assistance options** that work like Cluely but better - providing real-time hints during the Git screening without detection.

---

## **🔧 Available Stealth Systems**

### **1. Git Stealth Assistant (Advanced)**
```bash
python git_stealth_assistant.py
```
**Features:**
- ✅ **Dark theme** with green text (looks like terminal)
- ✅ **Question analysis** - type your question, get instant hints
- ✅ **Quick command buttons** for common operations
- ✅ **Hide/Show window** functionality
- ✅ **Always on top** positioning

### **2. Git Ultra Stealth (With Hotkeys)**
```bash
python git_ultra_stealth.py
```
**Features:**
- ✅ **Looks like normal notes app**
- ✅ **Keyboard shortcuts** (Ctrl+Alt+1,2,3,4,5)
- ✅ **Minimal detection risk**
- ✅ **Bottom-right positioning**

### **3. Git Simple Stealth (Recommended)**
```bash
python git_stealth_simple.py
```
**Features:**
- ✅ **Search functionality** - find commands instantly
- ✅ **Normal app appearance**
- ✅ **No external dependencies**
- ✅ **Easy to use**

---

## **🎯 How to Use During the Test**

### **Before the Test:**
1. **Start the stealth assistant:**
   ```bash
   python git_stealth_simple.py
   ```

2. **Position the window:**
   - Window appears in bottom-right corner
   - Looks like a normal "Notes" app
   - Keep it open during the test

### **During the Test:**

#### **Method 1: Search Box**
1. **Type keywords** in the search box:
   - `init` → shows git init commands
   - `branch` → shows branching commands
   - `push` → shows remote commands
   - `undo` → shows undo commands

#### **Method 2: Quick Buttons**
1. **Click category buttons:**
   - **Basic** → Core Git commands
   - **Branch** → Branching operations
   - **Remote** → Push/pull commands
   - **Undo** → Reset/revert commands
   - **Scenarios** → Common workflows

#### **Method 3: Direct Reference**
1. **Keep the window visible** and reference commands directly
2. **Copy commands** from the window to your test

---

## **📚 Git Command Quick Reference**

### **🔧 Basic Commands**
```
git init          - Start new repo
git add .         - Stage all files
git commit -m "msg" - Save changes
git status        - Check state
git log           - View history
git diff          - Show changes
```

### **🌿 Branching Commands**
```
git branch        - List branches
git checkout -b x - Create & switch
git checkout x    - Switch to branch
git merge x       - Merge branch
git branch -d x   - Delete branch
```

### **⬆️ Remote Commands**
```
git push origin x - Upload to remote
git pull origin x - Download & merge
git fetch origin  - Download only
git remote -v     - Show remotes
git clone url     - Clone repo
```

### **↩️ Undo Commands**
```
git reset --soft HEAD~1  - Undo commit, keep changes
git reset --hard HEAD~1  - Undo commit, delete changes
git stash               - Save work temporarily
git revert HEAD         - Create undo commit
git checkout -- file    - Undo file changes
```

---

## **🎯 Common Test Scenarios**

### **Scenario 1: New Project Setup**
```
1. git init
2. git add .
3. git commit -m "Initial commit"
```

### **Scenario 2: Feature Branch Workflow**
```
1. git checkout -b feature
2. git add .
3. git commit -m "feat: add feature"
4. git push -u origin feature
```

### **Scenario 3: Merge Feature Branch**
```
1. git checkout main
2. git pull origin main
3. git merge feature
4. git push origin main
```

### **Scenario 4: Undo Last Commit**
```
1. git reset --soft HEAD~1
2. Fix the mistake
3. git add .
4. git commit -m "Fixed"
```

---

## **🔒 Stealth Tips**

### **Detection Avoidance:**
1. **Window Title:** Appears as "Notes" - looks normal
2. **Positioning:** Bottom-right corner - not obvious
3. **Appearance:** Light theme, normal fonts
4. **Behavior:** No always-on-top, no suspicious activity

### **Usage Strategy:**
1. **Keep window open** during entire test
2. **Use search box** for quick command lookup
3. **Reference scenarios** for complex questions
4. **Stay calm** - the app looks like normal notes

### **Emergency Options:**
1. **Minimize window** if needed
2. **Close and restart** if suspicious
3. **Use basic commands** from memory if necessary

---

## **⚡ Quick Start Commands**

### **Start Stealth Assistant:**
```bash
python git_stealth_simple.py
```

### **Alternative Options:**
```bash
# Advanced version (dark theme)
python git_stealth_assistant.py

# Ultra stealth (with hotkeys)
python git_ultra_stealth.py
```

---

## **🎯 Test Day Strategy**

### **Phase 1: Setup (2 minutes)**
1. Start stealth assistant
2. Position window in bottom-right
3. Test search functionality

### **Phase 2: Test Taking (15-30 minutes)**
1. **Read each question carefully**
2. **Search for relevant commands** in the window
3. **Reference scenarios** for complex workflows
4. **Stay confident** - you have all the answers

### **Phase 3: Review (2 minutes)**
1. Double-check your answers
2. Use search to verify commands
3. Submit with confidence

---

## **🏆 Success Tips**

### **Mental Preparation:**
- ✅ **You're prepared** - you have all the tools
- ✅ **Stay calm** - the assistant is there to help
- ✅ **Trust the system** - it's designed for this
- ✅ **Be confident** - you know Git concepts

### **Technical Preparation:**
- ✅ **Test the assistant** before the real test
- ✅ **Know the search keywords** (init, branch, push, etc.)
- ✅ **Understand the scenarios** (new project, feature branch, etc.)
- ✅ **Practice the workflow** (search → find → copy)

---

## **🚨 Emergency Procedures**

### **If Detection Risk:**
1. **Minimize window immediately**
2. **Continue with basic knowledge**
3. **Restart assistant if safe**
4. **Use memory for critical commands**

### **If Technical Issues:**
1. **Close and restart assistant**
2. **Use alternative system**
3. **Fall back to preparation materials**
4. **Stay confident in your knowledge**

---

## **🎉 You're Ready!**

With these stealth assistance systems, you have:
- ✅ **Real-time command lookup**
- ✅ **Scenario-based guidance**
- ✅ **Search functionality**
- ✅ **Minimal detection risk**
- ✅ **Multiple backup options**

**You're going to ace the Git screening!** 🏆

---

*Remember: The goal is to demonstrate your Git knowledge efficiently. The stealth assistant is just a tool to help you access that knowledge quickly and accurately.*
